import utest.Assert;

class TestHtml {
  public function new () {}

/*
  public function testBasePath() {

    var emitter = new TestRouteEmitter();
    var router = new Routly(emitter);

    router.routes([
      "/" => function(?_) {
        Assert.isTrue(true);
      }  
    ]);

    router.listen(false);

    emitter.emit("/");

  }
*/
}
